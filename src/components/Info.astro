---
import Aside from "./ui/aside/Aside.astro";
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"aside"> {
  title?: string;
}

const { title, ...attrs } = Astro.props;
---

<Aside type="note" title={title ?? "信息"} {...attrs}>
  <slot />
</Aside>
